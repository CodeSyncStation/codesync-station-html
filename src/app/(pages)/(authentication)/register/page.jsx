import Registration from "@/Components/Authentication/Registration";
import React from "react";
export const metadata = {
  title: "Login || Digital Dropouts",
  description: "",
};

const page = () => {
  return (
    <>
      <Registration />
    </>
  );
};

export default page;

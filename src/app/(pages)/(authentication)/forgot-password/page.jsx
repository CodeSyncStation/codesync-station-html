import ForgotPassword from "@/Components/Authentication/Forgot";
import React from "react";
export const metadata = {
  title: "Forgot Passord || Digital Dropouts",
  description: "",
};

const page = () => {
  return (
    <>
      <ForgotPassword />
    </>
  );
};

export default page;
import ForgotPassword from "@/Components/form/Forgot";
export const metadata = {
  title: "Forgot Password || CodeSync station",
  description: "",
};

const page = () => {
  return (
    <>
      <ForgotPassword />
    </>
  );
};

export default page;